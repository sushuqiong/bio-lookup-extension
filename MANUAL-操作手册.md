# 生信快查 · 手把手操作手册

> 本手册写给"从来没装过浏览器扩展、也没上架过商店"的人。
> 分两部分：**第一部分**让你 3 分钟内自己用上；**第二部分**教你上架到商店给别人用。
> 卡在任何一步，把那一步的**屏幕截图**发我，我直接告诉你怎么点。

---

# 第一部分：先在 Edge 里装好自己用（3 分钟）

> ⚠️ 重点：**不上架商店，你也能马上自己用**。上架只是为了让别人也能一键安装。

## 第 1 步：解压扩展包

1. 打开桌面上的文件夹 `bio-lookup-submission`
2. 找到 `1-扩展包-上传商店.zip`
3. **右键**它 → 选「**全部解压缩…**」（Windows 11 显示为「提取全部」）
4. 弹出窗口里点「**提取**」→ 解压出一个新文件夹 `1-扩展包-上传商店`
5. 进去看一眼，里面应该是这些文件：`manifest.json`、`background.js`、`classify.js`、`content.js`、`popup.html`、`options.html`、`icons` 文件夹等

> 📌 记住这个文件夹的位置：`C:\Users\fengq\Desktop\bio-lookup-submission\1-扩展包-上传商店`

## 第 2 步：打开 Edge 的扩展管理页

1. 打开 **Microsoft Edge**
2. 在地址栏（最上面输入网址的地方）里输入：`edge://extensions` 然后按 **回车**
3. 你会看到一个「扩展」页面，**左边一列**有"已安装的扩展"等菜单

## 第 3 步：找到并打开「开发人员模式」（最容易卡住的一步）

> 请对照我给你画的示意图：`guide-edge-load.png`（就在本文件夹里，双击就能看）

1. 把鼠标移到页面**左侧那一列**，**往下滚动到最底部**
2. 在最底部会看到一个**开关**，旁边写着「**开发人员模式**」
   - ⚠️ 注意：中文 Edge 写的是"开发人员模式"（不是"开发者模式"）；如果你之前搜"开发者模式"当然找不到
   - 如果是英文界面，写的是 "Developer mode"
3. **把这个开关打开**（点一下，变成蓝色/打开状态）

打开成功后：页面**顶部/右上方会多出一排按钮**（打包扩展、加载解压缩的扩展程序、更新）。

## 第 4 步：加载我们的插件

1. 点顶部那排按钮里的「**加载解压缩的扩展程序**」（英文：Load unpacked）
2. 会弹出一个"选择文件夹"的窗口
3. 依次点：**桌面** → `bio-lookup-submission` → **单击选中** `1-扩展包-上传商店` 这个文件夹
   - ⚠️ 是**选中文件夹本身**（单击一下让它变蓝），**不要双击进去**！
4. 点窗口右下角的「**选择文件夹**」

## 第 5 步：确认装好了

1. 页面上会出现一张卡片：**「生信快查 · Bio Lookup 0.3.1」**，带一个图标 ✅
2. 点浏览器**右上角的拼图图标**（🧩 扩展按钮）→ 在列表里找到「生信快查」→ 点它旁边的**图钉**图标钉在工具栏上

## 第 6 步：测试（按这个清单点一遍）

| # | 操作 | 应该看到 |
|---|---|---|
| 1 | 随便打开一个有文字的网页（比如百度百科），**选中** `BRCA1` → 右键 | 菜单顶部出现「生信快查 · 🧬 基因「BRCA1」」+ NCBI Gene / Ensembl 等选项 |
| 2 | 选中三行文字 `TP53`、`rs80357906`、`GSE123456`（用鼠标拖选多行）→ 右键 | 菜单变成「📚 批量查询 3 条」+「各自动类型 · 打开首选库」+ 按类型分组 |
| 3 | 复制下面这段 VCF，粘贴到任意网页（如百度搜索框旁边不行，用记事本不行）→ 选中 → 右键<br>`17  7676154  rs80357906  C  T` | 菜单变成「🧾 VCF 1 条变异」+ gnomAD / ClinVar / VarSome 批量选项 |
| 4 | 点浏览器工具栏的插件图标 | 弹出深色面板：查询历史 + 批量查询两个标签 |
| 5 | 在面板顶部输入 `rs80357906` → 按回车 | 自动打开 dbSNP 页面，历史里多一条记录 |
| 6 | 面板底部点「⬇️ 导出 CSV」 | 下载一个 csv 文件（这条我之前没实测过，麻烦你重点验一下） |
| 7 | 面板右上角 ⚙️ → 打开「页面浮层」开关 → 弹窗点「允许」→ 回网页**双击** `rs80357906` | 鼠标旁弹出一个小卡片，可点库跳转 |

**任何一条不对**：把屏幕截图发我（最好截下右键时弹出的菜单），我修完给你新版本。

---

# 第二部分：上架到 Edge 商店（让别人一键安装）

> 全程免费。需要注册一个 Microsoft 账号（如果你有 Outlook / Hotmail 邮箱，那就是 Microsoft 账号）。

## A. 准备好 Microsoft 账号

- **如果你有** Outlook / Hotmail / Live 邮箱，或电脑上登录过 Microsoft 账号 → 直接用
- **如果完全没有**：
  1. 打开 https://signup.live.com
  2. 点「获取新的电子邮件地址」→ 自己起一个名字（如 `sushuqiong2026@outlook.com`）→ 设密码
  3. 按提示填国家（中国）、出生日期 → 完成验证（可能要手机收验证码）

## B. 进入微软开发者后台

1. 打开 https://partner.microsoft.com/dashboard/microsoftedge/overview
2. 点右上角「**登录**」→ 输入刚准备好的 Microsoft 账号和密码
3. **首次登录会要求填一些账号资料**（这是正常的，照实填即可）：
   - 国家/地区：**中国**
   - 账号类型：选「**个人 / Individual**」
   - **发布者名称（Publisher name）**：这个名字会显示在商店页面上，建议填「**树鸡的生信代码**」或「**sushuqiong**」（填好一般不能随便改，想清楚）
   - 可能要验证邮箱（去邮箱点确认链接）和手机号
4. 填完提交，进入后台首页

## C. 创建扩展并上传

1. 在后台找到「**Create new extension**」/「**新建扩展**」按钮，点它
2. 弹出上传窗口 → 点「选择文件」→ 选桌面 `bio-lookup-submission` 里的 **`1-扩展包-上传商店.zip`** → 上传
3. 等一会，系统会解析包并显示扩展名称（生信快查 · Bio Lookup）→ 进入信息填写页

## D. 逐项填表单

> 打开 `5-提交文案与步骤.md`，**每一栏都是"复制→粘贴"**，不用自己写。

要填的栏目与对应位置：

| 表单栏 | 从 `5-提交文案与步骤.md` 复制哪一段 |
|---|---|
| 显示名称 / Name | 第二节「显示名称」 |
| 简短描述 / Short description | 第二节「简短描述」 |
| 详细描述 / Description | 第三节「详细描述」（**整段复制**，很长没关系） |
| 分类 / Category | 第二节「分类」→ 选「生产力 / Productivity」 |
| 语言 / Language | 勾选「中文（简体）」和「English」 |
| 搜索关键词 / Search terms | 第二节「搜索关键词」（最多 7 个） |
| 商店截图 / Screenshots | 上传 `2-截图-1280x800` 里的 **4 张全部** |
| 小促销图 / Small promo tile | 上传 `3-促销图\小促销图-440x280.png` |
| 大促销图 / Large promo tile | 上传 `3-促销图\大促销图-1400x560.png` |
| 图标 / Icon | 上传 `4-图标\icon-128.png` |
| 隐私政策 URL | 粘贴 `https://sushuqiong.github.io/bio-lookup-extension/privacy.html` |
| 官方网站 / Website | 粘贴 `https://sushuqiong.github.io/bio-lookup-extension/` |
| 支持 URL / Support | 粘贴 `https://github.com/sushuqiong/bio-lookup-extension/issues` |
| 权限用途说明 | 复制第四节表格里对应权限的文字 |
| 数据收集声明 / Data collection | 按第五节全部选 **No**（不收集数据） |

## E. 保存并提交

1. 每填完一栏点「保存 / Save」（表单分段保存，可以分几次填完）
2. 全部填完后点「**发布 / Publish**」或「**提交审核 / Submit for review**」
3. 状态会变成 **In review（审核中）**
4. **审核 1–7 个工作日**（Edge 通常比 Chrome 快）
5. 通过后你会收到邮件，并在后台看到商店链接：
   `https://microsoftedge.microsoft.com/addons/detail/xxxx`
   —— 把链接发任何人，点「获取」就能装，**自动更新，不需要开发人员模式**

## F. 如果被拒 / 卡住

- 拒稿邮件里会写明原因（最常见：隐私政策缺失、权限说明不清、描述与功能不符——**我们的材料已规避这些**）
- 按原因改，或直接把邮件截图发我，我帮你改材料后重新提交

---

# 常见问题

**Q：开发人员模式装上的插件，Edge 每次启动都提示"请禁用开发人员模式扩展"怎么办？**
A：点那个提示里的「关闭」/「不再显示」即可（不同版本措辞不同）。或者干脆不管它——点掉就行，插件照常用。

**Q：装了新版（比如我修完 bug 给你 v0.3.2），怎么更新？**
A：把新 zip 解压覆盖原文件夹 → 回到 `edge://extensions` → 点插件卡片上的**刷新图标**（🔄）即可。

**Q：上架后我自己还要用开发人员模式版本吗？**
A：不需要了。商店版装一次就够，自动更新。可以把开发人员模式那个版本删掉（卡片上的"删除"）再装商店版。

**Q：上架要花钱吗？**
A：**Edge 商店免费**。Chrome 商店要一次性 $5（约 36 元），可以以后再说。

**Q：我不想上架，只想给同事用**？
A：把 `bio-lookup-submission.zip` 整个发给他们，让他们按"第一部分"操作即可（每人 3 分钟）。上架的好处只是"一键装 + 自动更新 + 能被搜到"。
